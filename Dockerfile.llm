FROM python:3.12-slim

WORKDIR /app
COPY audit-llm /app/audit-llm
RUN pip install --no-cache-dir -U pip \
    && pip install --no-cache-dir -e /app/audit-llm

EXPOSE 8001
CMD ["uvicorn", "audit_llm.main:app", "--host", "0.0.0.0", "--port", "8001"]
