/** Must match `ThemeProvider` in `components/console/Providers.tsx`. */
export const AUDIT_THEME_STORAGE_KEY = "audit-ui-theme" as const;

export const AUDIT_THEME_DEFAULT = "dark" as const;

export const AUDIT_THEME_ENABLE_SYSTEM = true as const;
